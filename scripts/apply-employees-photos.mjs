import{execFileSync}from'node:child_process';import{readFileSync}from'node:fs';import postgres from'postgres';const env={};for(const line of readFileSync('.env','utf8').split(/\r?\n/)){const v=line.trim(),i=v.indexOf('=');if(i>0&&!v.startsWith('#'))env[v.slice(0,i).trim()]=v.slice(i+1).trim().replace(/^['"]|['"]$/g,'')}const ref=new URL(env.SUPABASE_URL).hostname.split('.')[0],password=execFileSync('osascript',['-e','display dialog "Введите пароль базы Supabase для сотрудников и фото заказов" default answer "" with hidden answer buttons {"Отмена", "Продолжить"} default button "Продолжить"','-e','text returned of result'],{encoding:'utf8'}).trim();if(!password)throw new Error('Пароль базы не введён.');const sql=postgres(`postgresql://postgres.${ref}:${encodeURIComponent(password)}@aws-1-ap-south-1.pooler.supabase.com:5432/postgres`,{ssl:'require',max:1,connect_timeout:15,onnotice:()=>{}});try{await sql.unsafe(readFileSync('supabase/migrations/202608040013_employees_and_order_photos.sql','utf8'));console.log('EMPLOYEES_AND_PHOTOS=READY')}finally{await sql.end({timeout:5})}
