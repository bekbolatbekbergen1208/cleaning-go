import { requireAdmin } from '../../lib/require-admin'; import { resolveDispute,toggleReview } from './actions';
export default async function Moderation(){const db=await requireAdmin();const[{data:reviews},{data:disputes}]=await Promise.all([db.from('reviews').select('id,rating,text,is_visible,created_at').order('created_at',{ascending:false}).limit(100),db.from('disputes').select('id,reason,description,status,resolution,created_at').order('created_at',{ascending:false}).limit(100)]);return <div className="grid gap-8 lg:grid-cols-2"><section><h1 className="mb-5 text-3xl font-black">Отзывы</h1><div className="space-y-3">{reviews?.map(x=><article className="card" key={x.id}><b>{'★'.repeat(x.rating)}</b><p className="my-2 text-sm text-slate-600">{x.text||'Без текста'}</p><form action={toggleReview}><input type="hidden" name="id" value={x.id}/><button className="button" name="visible" value={String(!x.is_visible)}>{x.is_visible?'Скрыть':'Восстановить'}</button></form></article>)}</div></section><section><h1 className="mb-5 text-3xl font-black">Споры</h1><div className="space-y-3">{disputes?.map(x=><article className="card" key={x.id}><b>{x.reason}</b><p className="my-2 text-sm text-slate-600">{x.description}</p><p className="mb-3 text-xs font-semibold text-brand-700">{x.status}</p><form action={resolveDispute} className="grid gap-2"><input type="hidden" name="id" value={x.id}/><textarea className="input" name="resolution" placeholder="Решение" required defaultValue={x.resolution??''}/><select className="input" name="status"><option value="under_review">На рассмотрении</option><option value="resolved_client">Решено в пользу клиента</option><option value="resolved_cleaner">Решено в пользу клинера</option><option value="resolved_company">Решено в пользу компании</option><option value="closed">Закрыт</option></select><button className="button">Сохранить</button></form></article>)}</div></section></div>}
