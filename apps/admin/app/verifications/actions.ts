'use server'; import { revalidatePath } from 'next/cache'; import { requireAdmin } from '../../lib/require-admin';
export async function verifyCleaner(form:FormData){const db=await requireAdmin();const userId=String(form.get('userId'));const status=String(form.get('status'));if(!['approved','rejected','blocked'].includes(status))throw new Error('Invalid status');const{error}=await db.from('cleaner_profiles').update({verification_status:status}).eq('user_id',userId);if(error)throw error;await db.from('admin_action_logs').insert({admin_id:(await db.auth.getUser()).data.user!.id,action:'verify_cleaner',entity_type:'cleaner',entity_id:userId,new_data:{status}});revalidatePath('/verifications')}
export async function verifyCompany(form:FormData){const db=await requireAdmin();const id=String(form.get('id'));const status=String(form.get('status'));if(!['approved','rejected','blocked'].includes(status))throw new Error('Invalid status');const{error}=await db.from('company_profiles').update({verification_status:status}).eq('id',id);if(error)throw error;await db.from('admin_action_logs').insert({admin_id:(await db.auth.getUser()).data.user!.id,action:'verify_company',entity_type:'company',entity_id:id,new_data:{status}});revalidatePath('/verifications')}
