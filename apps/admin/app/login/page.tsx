'use client'; import { createClient } from '../../lib/supabase/client'; import { useRouter } from 'next/navigation'; import { useState } from 'react';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const{error}=await createClient().auth.signInWithPassword({email,password});if(error)setError('Неверный email или пароль');else{router.replace('/admin');router.refresh()}}return <div className="mx-auto mt-20 max-w-md card"><h1 className="mb-1 text-2xl font-black">Вход администратора</h1><p className="mb-6 text-slate-500">Эта страница только для владельца Cleaning Go.</p><form onSubmit={submit} className="space-y-4"><input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Пароль" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="button w-full">Войти</button></form></div>}
