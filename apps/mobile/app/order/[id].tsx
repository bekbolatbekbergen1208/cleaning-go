import { Button, Card, Field, Screen, Title, formatMoney, s } from '@/components/ui'; import { supabase } from '@/lib/supabase'; import { useSessionStore } from '@/store/session'; import type { Order, OrderStatus } from '@cleaning-go/types'; import * as Location from 'expo-location'; import { useLocalSearchParams } from 'expo-router'; import { useEffect, useState } from 'react'; import { Alert, Text, View } from 'react-native';
const next:Partial<Record<OrderStatus,{status:OrderStatus;label:string}>>={accepted:{status:'on_the_way',label:'Начать маршрут'},on_the_way:{status:'arrived',label:'Я прибыл'},arrived:{status:'in_progress',label:'Начать уборку'},in_progress:{status:'completed_by_cleaner',label:'Уборка завершена'},completed_by_cleaner:{status:'completed',label:'Подтвердить завершение'}};
type Point={latitude:number;longitude:number;recorded_at:string};
export default function OrderDetail(){const{id}=useLocalSearchParams<{id:string}>();const role=useSessionStore(s=>s.profile?.role);const demoOrders=useSessionStore(s=>s.demoOrders);const updateDemo=useSessionStore(s=>s.updateDemoOrderStatus);const demo=process.env.EXPO_PUBLIC_DEMO_MODE==='true';const[order,setOrder]=useState<Order|undefined>(demo?demoOrders.find(x=>x.id===id):undefined);const[point,setPoint]=useState<Point>();const[rating,setRating]=useState('5');const[review,setReview]=useState('');const executor=role==='cleaner'||role==='company_cleaner'||role==='company_owner';async function load(){if(demo){setOrder(demoOrders.find(x=>x.id===id));return}const{data,error}=await supabase.from('orders').select('*').eq('id',id).single();if(error)Alert.alert('Ошибка',error.message);else setOrder(data as Order)}useEffect(()=>{void load();if(demo)return;const c=supabase.channel(`order-${id}`).on('postgres_changes',{event:'UPDATE',schema:'public',table:'orders',filter:`id=eq.${id}`},p=>setOrder(p.new as Order)).on('postgres_changes',{event:'*',schema:'public',table:'cleaner_locations',filter:`order_id=eq.${id}`},p=>setPoint(p.new as Point)).subscribe();void supabase.from('cleaner_locations').select('latitude,longitude,recorded_at').eq('order_id',id).maybeSingle().then(({data})=>data&&setPoint(data));return()=>{void supabase.removeChannel(c)}},[id,demo,demoOrders]);
async function transition(status:OrderStatus){if(demo){updateDemo(id,status);setOrder(current=>current?{...current,status}:current);Alert.alert('Статус обновлён',`Новый статус: ${status}`);return}if(status==='on_the_way'){const permission=await Location.requestForegroundPermissionsAsync();if(permission.status!=='granted')return Alert.alert('Нужно разрешение','Маршрут запускается только с доступом к геолокации.');const loc=await Location.getCurrentPositionAsync({accuracy:Location.Accuracy.Balanced});const{error:le}=await supabase.rpc('update_cleaner_location',{target_order_id:id,lat:loc.coords.latitude,lng:loc.coords.longitude,target_heading:loc.coords.heading,target_speed:loc.coords.speed});if(le)return Alert.alert('Геолокация',le.message)}const{error}=await supabase.rpc('transition_order_status',{target_order_id:id,next_status:status,note:null});if(error)Alert.alert('Переход недоступен',error.message);else void load()}
async function sendReview(){if(demo){Alert.alert('Спасибо','Отзыв опубликован в демо-режиме.');return}const{error}=await supabase.rpc('create_review',{target_order_id:id,target_rating:Number(rating),target_text:review,target_tags:['рекомендую']});Alert.alert(error?'Ошибка':'Спасибо',error?.message??'Отзыв опубликован')}
if(!order)return <Screen><Text>Загрузка заказа…</Text></Screen>;const action=next[order.status];const showAction=action&&((executor&&action.status!=='completed')||(!executor&&action.status==='completed'));return <Screen><Title subtitle={`Статус: ${order.status}`}>№ {order.order_number}</Title><Card><Text style={s.cardTitle}>{order.address_text}</Text><Text>{new Date(order.scheduled_at).toLocaleString('ru-KZ')}</Text><Text>{order.area_sq_m} м² · {order.rooms_count} комн.</Text><Text style={s.cardTitle}>{formatMoney(order.total_minor)}</Text></Card>{point?<Card><Text style={s.cardTitle}>Клинер на маршруте</Text><Text>{point.latitude.toFixed(5)}, {point.longitude.toFixed(5)}</Text><Text style={s.muted}>Обновлено {new Date(point.recorded_at).toLocaleTimeString('ru-KZ')}</Text></Card>:['on_the_way','arrived','in_progress'].includes(order.status)?<Card><Text>Ожидаем первую точку геолокации…</Text></Card>:null}{showAction?<Button title={action.label} onPress={()=>void transition(action.status)}/>:null}{!executor&&['created','searching'].includes(order.status)?<Button title="Отменить заказ" variant="danger" onPress={()=>void transition('cancelled')}/>:null}{!executor&&order.status==='completed'?<Card><Text style={s.cardTitle}>Оцените уборку</Text><View style={s.row}>{[1,2,3,4,5].map(x=><Button key={x} title={String(x)} variant={rating===String(x)?'primary':'secondary'} onPress={()=>setRating(String(x))}/>)}</View><Field label="Отзыв" value={review} onChangeText={setReview}/><Button title="Опубликовать отзыв" onPress={()=>void sendReview()}/></Card>:null}</Screen>}
